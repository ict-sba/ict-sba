<!DOCTYPE html>
<html>
<head>
	<title>Quadratic Equation Graph</title>
	<style>
		canvas {
			border: 1px solid black;
		}
        
        
        .abc {
        background-color: white;
        border: 1px solid black;
        color: black;
        font-size: 15px;
        border-radius: 5px;
        padding: 5px;
        margin: 10px;
        align: center;
      }
      .abc:hover {
        background-color: black;
        color: white;
      }
      

 
       .center {
        margin-left: auto;
        margin-right: auto;
      }

      
      
	</style>
</head>
<body>
	<label>a:</label>
	<input type="text" id="a" value="1"><br>
	<label>b:</label>
	<input type="text" id="b" value="0"><br>
	<label>c:</label>
	<input type="text" id="c" value="0"><br><br>
	<canvas id="canvas" height=300px></canvas> <br>
        <table class="center">
<tr>
<th>
        <button class="abc" onclick="draw()">繪製圖像</button>
</th>
</tr>
</table>
	<script>
		function draw() {
			var a = parseFloat(document.getElementById("a").value);
			var b = parseFloat(document.getElementById("b").value);
			var c = parseFloat(document.getElementById("c").value);
			var canvas = document.getElementById("canvas");
			var context = canvas.getContext("2d");
			context.clearRect(0, 0, canvas.width, canvas.height);
			context.beginPath();
			context.strokeStyle = "black";
			context.moveTo(0, canvas.height / 2);
			context.lineTo(canvas.width, canvas.height / 2);
			context.stroke();
			context.beginPath();
			context.moveTo(canvas.width / 2, 0);
			context.lineTo(canvas.width / 2, canvas.height);
			context.stroke();
			context.beginPath();
			for (var i = -canvas.width / 2; i < canvas.width / 2; i++) {
				var x = i / 20;
				var y = a * x * x + b * x + c;
				var pixelX = canvas.width / 2 + i;
				var pixelY = canvas.height / 2 - y * 20;
				if (i == -canvas.width / 2) {
					context.moveTo(pixelX, pixelY);
				} else {
					context.lineTo(pixelX, pixelY);
				}
			}
			context.stroke();
		}
        
        
       
	</script>
</body>
</html>
